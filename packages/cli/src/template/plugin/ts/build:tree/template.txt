import { type BuilderPlugin } from '@obsidian_blogger/build_system'

// -------------------------------------------------------------- //
// ------------------------ Tree Plugin ------------------------- //
// -------------------------------------------------------------- //

interface {{plugin_name}}TreeBuilderPluginConfig {
    // Add your configuration here
    option1: string
    option2: number
}
export const {{plugin_name}}TreeBuilder = (
    config: {{plugin_name}}TreeBuilderPluginConfig
): BuilderPlugin['build:tree'] => {
    const { option1, option2 } = config

    return async ({
        buildPath,
        io,
        logger,
        meta,
        pathGenerator,
        disableCorePlugins,
    }) => {
        return {
            name: '{{plugin_name}}TreeBuilder',
            disableCache: false,
            skipFolderNode: true,
            exclude: [],
            walker: async (node, i, children) => {},
        }
    }
}
